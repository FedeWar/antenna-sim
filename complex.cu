#include "complex.cuh"
/*
namespace flib {

__device__ complex::complex(float _r, float _i) :
	r(_r), i(_i)
{}
	
__device__ complex::complex(const complex& _c) :
	r(_c.r), i(_c.i)
{}

__device__ complex::~complex()
{}

__device__ complex complex::operator+(const complex& c) const
{
	return complex(r + c.r, i + c.i);
}

__device__ complex complex::operator-(const complex& c) const
{
	return complex(r - c.r, i - c.i);
}

__device__ complex complex::operator*(const complex& c) const
{
	return complex(r * c.r - i * c.i, r * c.i + i * c.r);
}

__device__ complex complex::operator*(float a) const
{
	return complex(r * a, i * a);
}
*/
}	/* namespace flib */
