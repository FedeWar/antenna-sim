
namespace flib {

// Definisce il delta per le comparazioni tra i numeri in virgola mobile.
/*#ifndef DELTA
#define DELTA 0.0001f
#endif

class complex
{
	public:
		__device__ complex(float _r, float _i);
		__device__ complex(const complex& _c);
		__device__ ~complex();

		__device__ complex operator+(const complex& c) const;
		__device__ complex operator-(const complex& c) const;
		__device__ complex operator*(const complex& c) const;
		__device__ complex operator*(float a) const;

		// TODO implementare

		__device__ complex& operator+=(const complex& c);
		__device__ complex& operator-=(const complex& c);
		__device__ complex& operator*=(const complex& c);
		__device__ complex& operator*=(float a);

		__device__ complex conj() const;
		__device__ complex exp() const;

	private:
		float r;
		float i;
};
*/
// const complex j(0, 1);

}	/* namespace flib */
